import { Title, Story, Props, Canvas , Preview, Subtitle, Description, Primary, ArgsTable, Stories, PRIMARY_STORY, } from '@storybook/addon-docs/blocks';
import * as sassVariables from '../../assets/js/sassVariables.js';
const { textColors } = sassVariables

import CreCard from '../../components/UI/CreCard.vue';
import CreText from '../../components/UI/CreText.vue';
import CreButton from '../../components/UI/CreButton.vue';

<Meta title="Components/Card" component={ CreCard }
  parameters={{
    docs:{
      description: {
        component: "Use Crewotive’s Card component for actions in forms, dialogs, and more with support for multiple sizes, states, and more."
      },
    }
  }}
  decorators={[() => ({ template: '<div class=""><story/></div>' })]}
  argTypes={{
    textVariant: {
      description: "Text color variant of the card",  control: { type : 'select', options : textColors.filter(val=>val.match(/-\d$/g)==null) } },
    imgOverlay:{
      description: "Turn an image into a card `background` and overlay your card’s text."},
    noHeader:{
      description: "If checked, hides header section of the card component"},
    imgSrc:{
      description: "Image src attribute of the card component"}
  }}
  args={{
    imgSrc: "https://picsum.photos/500/300",
    imgAlt: "Card header image",
  }}
  />

export const Template = (args, { argTypes, ...vm }) => ({
  props: Object.keys(vm.parameters.component.props),
  components: { CreCard, CreText, CreButton },
  template: `
  <CreCard
    style="width: 20rem"
    v-radius.5
    v-bind="$props"
    >
    <div slot="card-header">
      <h2>Awesome card header</h2>
    </div>
    <div slot="card-body">
      <CreText el=h5 class="card-title">Card title</CreText>
      <CreText el=p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</CreText>
    </div>
    <div slot="card-footer">
      <CreButton variant="success" tint="6" label="Accept" />
      <CreButton variant="danger" tint="6" label="Denie" />
    </div>
  </CreCard>
  `
});

<div className="d-none">

<Story
  name="Card">
  {Template.bind({})}
</Story>

</div>


<Title/>
<SubTitle/>
<Description/>
<Primary/>
<ArgsTable story="Card" />

### SASS
#### Variables
```SCSS
$card-spacer-y:                     $spacer;
$card-spacer-x:                     $spacer;
$card-title-spacer-y:               $spacer / 2;
$card-border-width:                 $border-width;
$card-border-radius:                $border-radius;
$card-border-color:                 rgba($black, .125);
$card-inner-border-radius:          subtract($card-border-radius, $card-border-width);
$card-cap-padding-y:                $card-spacer-y / 2;
$card-cap-padding-x:                $card-spacer-x;
$card-cap-bg:                       rgba($black, .03);
$card-cap-color:                    null;
$card-height:                       null;
$card-color:                        null;
$card-bg:                           $white;
$card-img-overlay-padding:          $spacer;
$card-group-margin:                 $grid-gutter-width / 2;
```


<Stories/>