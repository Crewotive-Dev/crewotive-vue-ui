import { Title, Story, Props, Canvas , Preview, Subtitle, Description, Primary, ArgsTable, Stories, PRIMARY_STORY, } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import CreButton from '../../components/UI/CreButton.vue';
import * as sassVariables from '@/assets/js/sassVariables.js';
const { themeColors, colorTints } = sassVariables

<Meta title="Components/Buttons" component={ CreButton }
  parameters={{
    docs:{
      description: {
        component: "Use Crewotive’s  button component for actions in forms, dialogs, and more with support for multiple sizes, states, and more."
      },
    }
  }}
  decorators={[() => ({ template: '<div class="d-flex justify-content-center"><story/></div>' })]}
  argTypes={{
    label: { control: { type: 'text'} },
    variant: { control: { type : 'select', options : [...themeColors.filter(val=>val.match(/-\d$/g)==null),'link'] } },
    size: { control: { type : 'select', options : ['sm', 'md', 'lg'] } },
    tint: { control: { type : 'range', min : parseInt(colorTints[0]), max : parseInt(colorTints[colorTints.length-1])} },
    click: { action: 'buttonClick' }
  }}
  />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CreButton },
  template: `<cre-button v-bind="$props" @click="buttonClick"/>`,
  methods: {
    buttonClick: action('click')
  }
});

<div className="d-none">

  <Story
    name='Button' args={{
    label : "Button",
    variant : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Outline' args={{
    label : "Button",
    outline: true,
    variant : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Link' args={{
    label : "Button",
    outline: true,
    variant : "link",
    size : "md",
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Disabled' args={{
    label : "Button",
    disabled: true,
    variant : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Small' args={{
    label : "Button",
    variant : "primary",
    size : "sm",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Medium' args={{
    label : "Button",
    variant : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story
    name='Large' args={{
    label : "Button",
    variant : "primary",
    size : "lg",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

</div>

<Title/>
<SubTitle/>
<Description/>
<Primary/>
<ArgsTable of={PRIMARY_STORY} />
<Stories/>
