import { Title, Story, Props , Preview, Subtitle, Description, Primary, ArgsTable, Stories, PRIMARY_STORY, } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions'
import CreButton from '../components/UI/CreButton.vue';
import * as sassVariables from './assets/variables.js';
const { themeColors, colorTints } = sassVariables

<Meta title="Components/Buttons" component={CreButton} 
  parameters={{
    docs:{
      description: {
        component: "Use Crewotive’s  button component for actions in forms, dialogs, and more with support for multiple sizes, states, and more."
      },
    }
  }}
  argTypes={{
    label: { control: { type: 'text'} },
    color: { control: { type : 'select', options : [...themeColors.filter(val=>val[0].match(/-\d$/g)==null).map(e=>e[0]),'link'] } },
    size: { control: { type : 'select', options : ['sm', 'md', 'lg'] } },
    tint: { control: { type : 'number', min : colorTints.map(e=>e[0])[0], max : colorTints.map(e=>e[0]).reverse()[0]} },
  }}
  />

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { CreButton },
  template: `<cre-button v-bg.primary.7.gradient v-bind="$props" @click="clicked"/>`,
  methods: {
    clicked: action(`click`)
  }
});

<div className="d-none">

  <Story 
    name='Button' args={{
    label : "Button",
    color : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story 
    name='Outline' args={{
    label : "Button",
    outline: true,
    color : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story 
    name='Disabled' args={{
    label : "Button",
    disabled: true,
    color : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story 
    name='Small' args={{
    label : "Button",
    color : "primary",
    size : "sm",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story 
    name='Medium' args={{
    label : "Button",
    color : "primary",
    size : "md",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

  <Story 
    name='Large' args={{
    label : "Button",
    color : "primary",
    size : "lg",
    tint : "5"
    }}>
    {Template.bind({})}
  </Story>

</div>

<Title/>
<SubTitle/>
<Description/>
<Primary/>
<Props />
<Stories/>
